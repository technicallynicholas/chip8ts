class Memory {
  constructor() {}

  createMemory(size: number) {}

  readMem(address: number, length: number) {}

  writeMem(address: number, length: number) {}

  copyMem(source: number, dest: number, length: number) {}
}

export default Memory;
